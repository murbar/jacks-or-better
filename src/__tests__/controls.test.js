// import { withTheme } from './app.test';
import Controls from 'components/Controls';

import { render, fireEvent, cleanup } from '@testing-library/react';

it('renders', () => {
  // const state = {
  //   gameState: {
  //     busy: false,
  //     didDeal: false,
  //     didScore: false
  //   },
  //   actions: {
  //     incrementBet: jest.fn(),
  //     setMaxBet: jest.fn(),
  //     play: jest.fn()
  //   }
  // };
  // const { getByTestId } = render(withTheme(Controls, state));
});
